import cv2
import numpy as np


def safe_imdecode(jpg_bytes):
    try:
        return cv2.imdecode(np.frombuffer(jpg_bytes, dtype=np.uint8), cv2.IMREAD_COLOR)
    except:
        return None
